# Abbreviations (expand inline so the full command shows in history)
abbr -a nv nvim
abbr -a e 'emacsclient -nw'
abbr -a m mutt
abbr -a tss 'tailscale status'
abbr -a tsu 'tailscale up'
abbr -a tsd 'tailscale down'
